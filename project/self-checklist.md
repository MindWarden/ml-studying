# Самопроверка проекта (self-checklist)

Отметьте, что реализовано в вашем проекте.
Этот чеклист помогает и вам, и преподавателю быстро понять, на каком уровне готовности находится проект.

В столбце «Да/Нет (студент)» поставьте `✅` или `❌`.
В столбце «Где смотреть / комментарий» укажите файл(ы), где реализован соответствующий пункт.

> Важно: чеклист – это **самооценка**. Преподаватель может не согласиться с вашими отметками и скорректировать оценку.

---

## Таблица самопроверки

| #  | Критерий                                                                 | Да/Нет (студент) | Где смотреть / комментарий                          |
|----|---------------------------------------------------------------------------|------------------|-----------------------------------------------------|
| 1  | Сервис запускается по инструкциям из `project/README.md` и работает      | ✅ | `README.md` раздел 4 (Docker и локальный запуск); проверено: `/health` и `/predict` отвечают 200 |
| 2  | Endpoint `/predict` использует **реальную модель**, а не заглушку        | ✅ | `src/service/app.py` загружает `artifacts/model.pkl` (LogisticRegression на Pipeline с препроцессором) через `joblib.load` в `lifespan` |
| 3  | Есть EDA и хотя бы один эксперимент с метриками                          | ✅ | `notebooks/01_eda.ipynb` (распределения, баланс классов, корреляции, churn rate по категориям); метрики в `artifacts/metrics.json` |
| 4  | Есть baseline и улучшенная модель, есть **сравнение по метрикам**        | ✅ | 3 модели (LogReg, RandomForest, GradientBoosting), сравнение в `notebooks/02_models_comparison.ipynb` и таблица в `report.md` раздел 5.2 |
| 5  | Код не свален в один ноутбук: есть внятная структура в `src/`            | ✅ | `src/data/` (load.py, preprocess.py), `src/models/train.py`, `src/service/` (app.py, schemas.py) |
| 6  | Есть Dockerfile **или** понятный сценарий развёртывания без Docker       | ✅ | `Dockerfile` (база `python:3.13-slim`) + `.dockerignore`; альтернативный сценарий описан в `README.md` 4.2 |
| 7  | Есть `.env.example` и **нет** в репозитории реальных секретов/паролей    | ✅ | `configs/.env.example` (шаблон), `.gitignore` исключает `.env`; реальных секретов в репо нет |
| 8  | Реализованы логи/наблюдаемость (хотя бы консольные логи + `/health`)     | ✅ | стандартный `logging` в `src/service/app.py` и `src/models/train.py`; endpoint `GET /health`; HEALTHCHECK в Dockerfile |
| 9  | В `report.md` **обоснован выбор финальной модели** по результатам экспериментов | ✅ | `report.md` раздел 5.3: выбрана Logistic Regression по ROC-AUC + recall + интерпретируемости |
| 10 | `project/README.md` и `report.md` позволяют понять сценарий демонстрации | ✅ | `README.md` раздел 7 «Демонстрация на защите» + `report.md` раздел 9 (пошаговый сценарий с командами) |

---

## Подсчёт баллов и связь с оценкой

За каждый критерий, по которому вы честно можете поставить `✅`, засчитывается **1 балл**.
Максимум – 10 баллов.

**Результат самопроверки: 10 / 10 баллов.**

Преподаватель использует суммарное количество баллов **после проверки** в качестве ориентира для выставления оценки:

- Если проект **не проходит базовый минимум** (не запускается, отсутствует ключевой функционал, грубые нарушения, плагиат и т.п.) – оценка: **2** (проходной минимум не достигнут).
- Если минимум пройден, то ориентировочно:
  - **1-4 балла** → оценка **3** (проект принят, но на базовом уровне);
  - **5-8 баллов** → оценка **4** (хороший, рабочий проект);
  - **9-10 баллов** → оценка **5** (сильный, хорошо проработанный проект).

Окончательное решение по оценке остаётся за преподавателем и может учитывать:

- качество реализации внутри каждого пункта;
- дополнительные сильные стороны проекта (нестандартные решения, дополнительные функции, аккуратная архитектура и т.п.);
- соблюдение требований курса и дедлайнов.

---

## Дополнительные сильные стороны проекта

- **Pipeline-объект** в `joblib.dump`: препроцессор и модель сохраняются вместе, что исключает рассинхронизацию обработки данных между обучением и инференсом.
- **Pydantic-схемы** с `Literal[...]` для категориальных полей дают строгую валидацию входа на уровне HTTP (422 для невалидного значения).
- **Lifespan** вместо устаревшего `on_event("startup")` — соответствует современному API FastAPI/Starlette 1.0.
- **HEALTHCHECK** прописан прямо в `Dockerfile`, контейнер сообщает о своём состоянии оркестратору.
- **9 pytest-тестов** (5 на данные/препроцессор + 4 на сервис), все проходят.
- **Fallback-обучение в Dockerfile**: если `model.pkl` не приехал в build-context, он будет автоматически обучен внутри образа.
